// 编译测试（负向语法）：替换过滤器只与 replace 模式搭配。
// 期望解析诊断：替换过滤器只与 replace 模式一起使用。
namespace invalid_syntax_fill_filter;

fn f() {
    fill(pos(0, 64, 0), pos(1, 64, 1), block_state("minecraft:stone"), hollow, block_state("#minecraft:logs"));
}
