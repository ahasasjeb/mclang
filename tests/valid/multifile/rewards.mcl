// 编译测试：多文件项目（中文关键词），提供物品与奖励函数。
// 使用入口模块的 players 查询，因此要 import main。
命名空间 multifile;

import main;

物品 reward = 物品堆("minecraft:emerald") {
    数量 = 2;
    自定义名称 = "多文件奖励";
}

export 函数 award() {
    给予(players, reward);
    消息.全部("奖励已发放", 金色);
}
