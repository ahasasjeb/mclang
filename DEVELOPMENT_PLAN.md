# Mclang 开发计划

## 目标

用 Rust 实现一门面向 Minecraft Java Edition 26.3-rc-2 的数据包编程语言。编译器把 `.mcl` 源文件转换为可直接放入世界 `datapacks` 目录的数据包，并在编译期报告词法、语法和语义错误。

目标版本依据仓库内 `minecraft_client_26.3-rc-2/version.json` 固定为数据包格式 `121.0`。函数和函数标签目录依据该版本源码中的 `ServerFunctionLibrary` 与 `Registries`，分别使用 `data/<namespace>/function` 和 `data/<namespace>/tags/function`。

## 方向修正：从命令包装器到数据包语言

- [x] 将实体查询、物品谓词、物品存储、维度上下文、实体操作和消息加入结构化 AST。
- [x] 检查资源位置、标签、范围、颜色、查询/存储引用以及实体函数上下文。
- [x] 保留 `run` 作为底层兼容边界，实际项目不再依赖命令字符串。
- [x] 增加 `--deny-raw`，让项目可以强制拒绝底层命令字符串。
- [x] 增加类型化声音调用和 predicate 布尔条件，使仓库示例全部通过严格模式。
- [x] 用零底层命令字符串的便携箱子项目完成编译级验收。
- [x] 支持中英文双关键词和同文件混用，并验证两种写法生成完全相同的产物。
- [x] 增加可复用物品堆定义、名称、Lore、附魔、损伤、无法破坏、`give_item` 和玩家执行上下文。
- [x] 完成 `give` 命令建模：玩家目标查询、显式数量、`最大堆叠数 × 100` 上限，以及物品名称、稀有度、模型、染色、光效覆盖、最大损伤和最大堆叠组件。
- [ ] 继续建立方块、坐标、玩家、物品、效果、声音、粒子和结构化 NBT 类型。
- [ ] 从 26.3 命令树和注册表生成版本化校验数据。

## 第一阶段：可用的端到端编译器

- [x] 建立清晰的 Rust 模块边界：词法分析、语法树、解析、语义检查、代码生成、命令行入口。
- [x] 定义最小语言：命名空间、全局计分变量、函数、`@load`/`@tick`、原生命令、函数调用、赋值、算术、条件分支、调度和返回。
- [x] 实现带文件名、行列和源码片段的诊断。
- [x] 生成 `pack.mcmeta`、`.mcfunction` 文件以及 load/tick 标签。
- [x] 自动生成计分板初始化命令和表达式临时值，保证函数与变量引用在编译期可检查。
- [x] 提供 `build` 与 `check` 命令，安全写入输出目录；默认输出到 `build/<项目名>`。

## 第二阶段：质量与可维护性

- [x] 为词法、解析、语义分析和数据包输出添加有意义的测试。
- [x] 加入端到端示例，并检查生成的目录、JSON 与 mcfunction 内容。
- [x] 运行 `cargo fmt`、`cargo test` 和 `cargo clippy`，修复全部项目内警告。
- [x] 编写语言参考、命令行说明、快速上手和生成规则文档。

## 第七阶段：语言一致性审查

- [x] 关键词表收敛为单一来源，保留字从关键词表派生，新增中英文别名的唯一性与双向解析测试。
- [x] 修正执行上下文模型为“无 / 任意实体 / 非玩家实体 / 玩家”：`data` 类 NBT 操作只允许非玩家实体上下文，新增 `@non_player`（中文 `@非玩家`）属性。
- [x] 把 `items(...)` 重命名为 `item_list(...)`，消除 `item` 与 `items` 共用中文别名 `物品` 的隐患；移除与 `remove` 生成完全相同的 `consume`。
- [x] 空槽来源从 `container.*` 改为 `group(hotbar.*, inventory.*)`，交还物品不再可能落进盔甲、副手或合成槽。
- [x] 补全 Mclang 与原生命令的完整对应关系表，并同步语言参考、语言设计、编译器设计与手册。
- [x] 代码生成在构造时预计算参数与局部变量假玩家表，移除重复的作用域扫描。

## 后续路线

- [x] 多源文件项目。
- [ ] 模块/import 系统。
- [x] 函数参数和 score 返回值。
- [ ] 基于 26.3 函数宏的高级调用。
- [x] 词法作用域局部变量。
- [ ] 实体上下文类型、存储/NBT 类型和复合数据。
- [x] `while` 和谓词组合。
- [ ] `for`、break/continue 和更强的控制流优化。
- [ ] 从目标 Minecraft 源码或命令树导出命令签名，提供静态命令校验和补全数据。
- [ ] 增量构建、源映射、语言服务器与编辑器集成。

## 第三阶段：项目化与可靠控制流

- [x] 让 `build`/`check` 接受目录，递归编译其中的 `.mcl` 文件并检查命名空间一致性。
- [x] 为源码范围加入文件身份，使跨文件词法、语法和语义错误准确指向原文件。
- [x] 支持 `!`、`&&`、`||` 条件组合，并保持确定的求值顺序。
- [x] 支持 `while` 循环，明确由 Minecraft `maxCommandChainLength` 提供运行时上限。
- [x] 分析同步函数调用图并拒绝递归环，消除全局临时计分项被重入覆盖的问题；调度调用不计入同步递归。
- [x] 增加多文件示例和针对新增关键行为的测试，更新用户文档。

## 第四阶段：数据包资源

- [x] 支持跨行原始字符串，便于在源码中嵌入可读 JSON。
- [x] 增加 `resource <类型> <名称> = <JSON>;` 顶层声明。
- [x] 根据 26.3-rc-2 注册表限制资源类型，并在编译期解析和格式化 JSON。
- [x] 检查重复资源并输出到 `data/<namespace>/<类型>/<名称>.json`。
- [x] 增加谓词资源示例、验证测试和语言文档。

## 第五阶段：函数内状态

- [x] 支持函数参数和表达式实参，定义稳定的计分板调用约定。
- [x] 支持词法块作用域的 `let` 局部计分变量。
- [x] 检查参数数量、入口参数、调度参数、重复局部名和声明前使用。
- [x] 为参数与局部变量分配不会超过 Minecraft 限制的内部假玩家名称。
- [x] 更新跨文件示例、语言参考和代码生成测试。

## 第六阶段：函数返回值

- [x] 增加 `fn name(...) -> score` 和 `return <表达式>;` 语法。
- [x] 允许返回值函数调用出现在算术表达式和实参中。
- [x] 使用 `execute store result score ... run function` 实现计分返回 ABI。
- [x] 检查 void/value 返回匹配、缺失返回、入口返回值和表达式调用类型。
- [x] 禁止嵌套结构块中的 return，避免 Minecraft 辅助函数改变控制流含义。
- [x] 把表达式调用纳入同步递归分析，补齐示例、测试与文档。

第一阶段强调完整而小的闭环。原生 Minecraft 命令保留逃生口，语言结构负责消除常见的函数路径、计分板和条件执行样板；后续能力可以在不破坏已有源码的前提下扩展。
