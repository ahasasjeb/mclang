# Mclang 语言设计规范

本文件约束 **Mclang 后续语言能力的设计**，目的是让语法、语义、诊断和产物保持同一种风格。它不是语法手册：当前可用的具体写法、参数范围和示例以 [`docs/content/manual.md`](docs/content/manual.md) 为准；Minecraft 的资源类型、命令签名和 codec 以仓库固定的 Java Edition 26.3 源码及 `data/version/26.3/` 快照为准。尚未实现的提议不能只凭本文件当作已支持的功能。

本规范使用「必须」表示新增能力的验收条件，「应」表示默认设计方向。项目仍处于原型期；确需修改既有设计时，可以直接修改语法与本规范，同时更新手册、示例和语料，不为旧写法自动添加兼容层。

## 1. 语言的边界

Mclang 的交付物是**可由 Minecraft 加载的数据包**。作者写 `.mcl` 描述数据包中的声明、资源和行为；编译器在写文件前检查能够静态确定的错误，再生成 `.mcfunction`、JSON、标签与包元数据。编译器不模拟整个游戏世界，也不能保证运行时目标实体、方块或外部数据包资源一定存在。

| 层 | 现有模型 | 扩展时必须保持的边界 |
| --- | --- | --- |
| 项目 | `main.mcl` 为入口；按 `import` 到达模块，顶层声明默认私有，`export` 后可导入 | 声明的可见性、输出路径和重名规则必须可预测 |
| 编译期数据 | 查询、物品堆、坐标形状、NBT、资源引用等由专门语法表示 | 不把这些概念伪装成运行期 `score` 值 |
| 运行期数据 | 当前表达式与普通函数参数、返回值以 32 位计分整数为核心 | 新值类型必须先说明存储、求值、传参及失败语义 |
| 执行上下文 | 无实体、任意实体、非玩家实体、玩家 | 需要 `@s` 或玩家/非玩家能力的操作必须静态检查 |
| 原版边界 | 命令和资源最终遵循固定目标版本的规则 | 原版不允许或编译器尚未验证的形状不能宣称已经安全 |

设计优先顺序：**保住作者可理解的语义 → 在编译期发现可确定的错误 → 映射到目标版本的原版机制 → 保持产物稳定且可检查**。性能优化只能在这些语义不变的前提下进行。例如，条件常量折叠不能改变有副作用表达式的求值次数；辅助函数内联不能改变 `return` 或 `execute store` 的结果。

## 2. 语法与命名

1. 沿用已有的视觉语法：顶层用声明，行为用语句，产生计分值的操作可出现在表达式里；参数使用 `(...)`，作用域或结构化字段使用 `{...}`，完整语句和字段以 `;` 结束。新的结构首先寻找已有语法位置，不为同一个概念平行造一套语法。
2. 作者可复用的事物应有声明和名字，例如 `query`、`item`、`objective`、`advancement`、`fn_tag`；临时操作优先沿用已支持的内联形式。产生数据包文件的声明必须说明文件路径、名字冲突和引用规则。
3. 沿用现有英文规范拼写：多词名用 `snake_case`，一组相关操作用 `领域.动作(...)`（如 `scoreboard.get`、`advancement.revoke`），作用于当前执行实体的操作用 `self.*`。新命名应描述用户动作或领域概念，避免直接暴露 `execute` 链、假玩家名等生成细节。
4. 同一概念只给一个首选入口。只有原版确有不同语义，或已有别名被项目明确接受时才增加同义写法；不同写法之间的参数、上下文和失败语义要一致。
5. 语法中的整数、小数、时间、坐标、资源位置、NBT、文本组件各有自己的领域意义。复用已有构造与校验器，例如 `pos`/`vec3`、`nbt`、`text`；不要让相同形状的参数在不同命令里偷偷采用不同默认值或范围。

### 中英双写法

- 每个新增的语言关键词、属性、方法与枚举值都必须有英文与中文写法；同一解析位置内的对应关系不得有歧义。全局保留的关键词须一一对应且全局唯一；方法和枚举按各自领域归一化，复用同一语义时尽量沿用已有译名。`src/parser/keywords/` 是关键词与别名的来源。两种写法可以在同一语句混用，生成的数据包必须逐字节相同。
- 只翻译**语言词汇**。用户声明的函数名、模块名、标准库 API 名、字符串内容、Minecraft 资源位置、NBT/JSON 中的原版字段和宏参数名不随关键词切换。新增词汇时，必须先判断它究竟是语言词，还是用户或原版的数据。
- 中文等非 ASCII 标识符可以作为声明名；编译器为产物分配稳定且无冲突的 ASCII 别名。诊断和编辑器展示源码名字。`namespace` 和需要直接符合原版/文件系统约束的路径仍按相应 ASCII 规则验证。

## 3. 模块、符号与资源引用

- 项目只有一个数据包命名空间。入口声明 `namespace`，子模块可继承；模块路径由项目内文件路径决定。跨模块使用公开声明要显式 `import`，内置 `std::` 也走导入规则。新增声明类型必须接入可见性、选择性导入、别名、重名和跨文件诊断，而不是另设全局查找捷径。
- 区分本地声明引用和外部原版/其他数据包引用。本地符号应能检查存在性、类别、参数、上下文以及生成路径；外部资源位置应验证格式，并在诊断中说明哪些内容无法由本项目确认。函数标签沿用 `#` 表示标签，不把普通函数与标签混为一类。
- 输出路径按模块与声明名推导：函数、JSON 资源、进度、函数标签等使用模块路径；计分、目标、查询等按各自现有命名规则。`__mcl`、内部 objective 和临时计分项属于编译器；用户名字不得撞上内部名字或另一份资源的输出路径。
- 引用一个查询时必须携带它的**基数约束**：允许多实体的操作可逐个执行；只接受一个实体的操作要求可静态确认单实体，例如查询显式 `limit(1)`。新增命令不能悄悄选取第一个目标。

## 4. 值、求值与控制流

- 现有运行期数值是 `score` 对应的 32 位有符号整数；`score` 是世界持久状态，`let` 是词法局部名，函数参数与返回值也是计分值。小数、字符串、NBT 和 JSON 目前主要出现在编译期构造或命令参数中，不因此成为通用运行期值。新类型必须同时定义字面量、变量、运算、比较、传参、返回、持久化和与原版命令交互的规则。
- 表达式用于**取得结果**，语句用于**执行动作**。允许命令结果进入表达式时，必须说清结果类型、无目标/失败时的值、是否有副作用，以及是否会执行多次。不要根据命令名字推断所有原版反馈都能安全地当作计分值。
- 实参从左到右求值；`&&`、`||` 从左到右短路。`for` 使用半开区间、动态终点只求值一次；`while` 每轮重算条件。常量折叠和代码生成必须保留这些求值次数与顺序。
- 块的词法作用域与运行时生成的辅助函数是两回事。`let`、循环变量和参数各有明确作用域，不能凭生成文件边界泄漏；`break`/`continue` 只作用于当前一层循环。现有 `return` 只允许在函数最外层，因为嵌套块下降为辅助函数；若未来放宽，必须先定义跨辅助函数的真实返回语义。
- 对除零、越界、无效枚举等静态已知情况直接诊断；运行期可能失败的原版命令应在文档中说明结果和状态是否保持原样。不要为方便生成而引入未说明的隐式截断、重置或重复执行。

## 5. 执行上下文与函数

执行上下文的能力关系如下；位置、朝向和维度的改变应另外描述，它们本身不改变 `@s` 的身份。

```text
无实体
  └─ 任意实体
       ├─ 非玩家实体
       └─ 玩家
```

- `@load`/`@tick` 没有执行实体；`@entity`、`@non_player`、`@player` 声明函数需求。`each` 按查询建立玩家或非玩家上下文，`spawn` 建立新召唤实体的非玩家上下文，结构化 `execute as/on/summon` 按实际子句改变上下文；`at`、`positioned`、`in_dimension` 等只改变执行位置或环境。新增块和命令必须明确输入上下文、输出上下文以及对 `@s` 的要求。
- 写玩家数据、实体 NBT、玩家消息或经验等操作必须按实际原版限制验证。无法静态证明目标类型时，应缩小能力或给出诊断；不能因为选择器在某个示例中恰好命中正确实体就当作类型保证。
- 普通函数调用检查实参数量、返回类型和上下文。函数的计分参数占用固定私有槽位；原版直接 `/function` 不会绑定这些参数。同步调用图包括条件、表达式、标签与结构块中的调用，拒绝同步递归；`schedule` 是未来游戏刻的调用，不保留当前实体上下文和普通实参。
- `macro fn` 使用原版函数宏机制，参数类型与普通计分参数分开；字面量调用检查键和类型。运行期 `with` 来源只能做有限静态验证。扩展宏时必须注明插入位置、转义/注入边界、上下文传播，以及严格模式是否接受。

## 6. 结构化命令与资源

新增原版能力时，优先给 Mclang 一个能表达其意义的 AST 形状，并按共同参数类型验证。一个结构化入口至少定义：接受什么目标、在什么上下文运行、参数的类型/范围/默认值、生成哪种原版命令、失败时怎样表现、能否进入表达式或 `return run`。复杂命令可使用 `领域.动作` 或结构化块；相同的资源位置、坐标、物品谓词、NBT 路径和文本组件应复用现有解析与校验规则。

数据包资源也属于语言的一等交付物。现有 `resource <类型> <名> = """JSON""";` 可输出多种资源，但字段级校验仍不完整；`advancement`、`fn_tag` 等已有更专门的声明。新增类型化资源语法时，应先确定 26.3 codec、必填字段、默认值、引用关系和输出路径，并让内联与独立声明共享验证规则。原始 JSON 路径必须明确已检查到哪一层，不能把 JSON 语法通过等同于 Minecraft 加载成功。

`run "..."`、字符串形式的 `execute` 和 `return run` 是命令逃生口；`--deny-raw` 还拒绝运行期 `with` 宏调用和 `nbt` 宏参数。结构化 `execute`、结构化 `return run` 与普通资源 JSON 不因名字中出现 `execute`、`run` 或 JSON 而自动计入该统计。新增逃生口必须在 AST 中可识别、可递归统计、可由严格模式诊断，不能藏在看似结构化的 API 后面。

## 7. 诊断、产物与文档

- 能在词法、语法、命名或语义阶段确定的问题，应在写出数据包之前报错；一次检查尽量返回全部相互独立的诊断。诊断使用源码中的名字和精确位置，说明实际值与期望值。`check` 与 LSP 应复用同一套分析结果。
- 生成文件必须可复现：相同项目输入和构建参数得到相同路径与字节，中英关键词转换也如此。遍历、辅助函数命名、JSON 字段顺序和冲突处理不能依赖非稳定顺序。生成的 `__mcl/load` 对已有持久计分和存储只补缺，不在 `/reload` 时悄悄清空状态。
- 版本敏感的资源、字段、枚举、命令签名和默认值须对照仓库内 26.3 源码/快照。新增语法或运行时行为时同步更新 [`docs/content/manual.md`](docs/content/manual.md)、必要的 [`DEVELOPMENT_PLAN.md`](DEVELOPMENT_PLAN.md) 条目和一个真实 `.mcl` 示例；手册页面由 `bun docs/tools/build.mjs --self-test` 生成与验证。

## 8. 新能力的设计检查表

在实现或评审一项语言变化时，把下列答案写进设计说明、代码评审或对应手册小节：

1. **用户概念**：这是声明、语句、表达式、条件、属性，还是现有结构的一个参数？为什么需要新入口？
2. **两种词汇**：英文规范名、中文对照、与现有词汇的关系是什么？哪些内容属于用户标识符或原版数据而不翻译？
3. **静态契约**：参数类型、范围、默认值、资源引用、目标基数、执行上下文、可见性和重名规则是什么？
4. **运行契约**：求值顺序、副作用、失败结果、持久状态、调度/宏边界，以及具体生成的原版命令或资源形状是什么？
5. **证据与限制**：依据哪份 26.3 源码或快照？哪些字段已经校验，哪些仍交给原版加载时检查？
6. **验收材料**：有效与无效 `.mcl` 语料、实际生成的 `.mcfunction`/JSON、中英产物逐字节对比、手册与示例是否一致？

通过这六项后再决定具体 AST、校验器与代码生成位置。若发现现有语法与这些规则冲突，先把冲突作为明确的语言设计决定记录并同步修改本规范，避免靠又一个特例掩盖它。
