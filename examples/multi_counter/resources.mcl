namespace multi_counter;

resource predicate coin_flip = """
{
  "type": "minecraft:random_chance",
  "chance": 0.5
}
""";

// 模块私有的函数：没有 export，其他模块不能调用。
fn random_message() {
    if predicate(coin_flip) {
        message.all("Heads");
    } else {
        message.all("Tails");
    }
}
